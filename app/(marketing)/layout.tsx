// app/(marketing)/layout.tsx
// Shared layout for marketing pages (landing, pricing)

export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}